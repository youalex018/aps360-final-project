[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "toxic-chat"
version = "0.1.0"
description = "APS360 toxic gaming chat classifier"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["toxic_chat*"]
